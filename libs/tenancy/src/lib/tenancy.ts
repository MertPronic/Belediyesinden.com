/**
 * Belediyesinden · tenancy library
 * Çok kiracılı (multi-tenant) izolasyon: tenant çözümleme, istek-bazlı search_path,
 * tenant provisioning/migration. Çözümleme için üst seviye `index.ts` barrel'ine bakın.
 */
export {};
