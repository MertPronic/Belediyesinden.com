/**
 * Belediyesinden · auth library
 * Keycloak entegrasyonu: token çıkarma, rol eşleme, decorator'ler, admin REST istemcisi,
 * kullanıcı senkronu. Çözümleme için üst seviye `index.ts` barrel'ine bakın.
 */
export {};
